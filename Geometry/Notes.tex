Area of a polygon is:
$$\frac{1}{2} \sum_{i = 1}^{n - 1} |p_i \times p_{i+1}| = \frac{1}{2}\sum_{i = 1}^{n - 1} |x_i y_{i+1} - x_{i+1}y_i|$$ $p_i$ is adjacent to $p_{i+1}$, and $p_1 = p_n$.

If no absolute, if area is smaller than 0 then points are listed CCW, otherwise CW, if equals 0 then 3 points are in a line. 


Point location to a segment. Point is $p$, segment from $a$ to $b$
$(p - a) \times (p - b) < 0, =0, >0$ : Left, Touch, Right
